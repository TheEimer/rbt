# @package _global_
defaults:
  - override /hydra/launcher: submitit_slurm

hydra:
  launcher:
    partition: normal
    cpus_per_task: 64
    mem_gb: 128
    gres: null
    # qos: express
    timeout_min: 240
    setup:
      - export JAX_PLATFORM_NAME=cpu